# Pin the public npm registry explicitly so Dependabot security updates on the
# root manifest (firebase-tools) don't infer a private GitHub Packages registry
# and abort with "can't access a private package registry without explicit
# configuration". We publish/consume nothing from GitHub Packages.
# See frontend/.npmrc and DEPLOY.md Appendix B.6.
registry=https://registry.npmjs.org/
